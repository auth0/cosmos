import React from 'react'
import styled from 'styled-components'

import { Heading1, List, Stack, Logo, Code, Button } from '../components'

const Link = styled.a`
  color: #0a84ae;
  text-decoration: none;
`

const clients = [
  {
    id: 'autogenerated-id-goes-here',
    name: 'Cleint One',
    logo: 'https://emojipedia-us.s3.amazonaws.com/thumbs/240/apple/118/robot-face_1f916.png',
    type: 'NO TYPE'
  },
  {
    id: 'beep-beep-boop-boop',
    name: 'Cleint Two',
    logo: 'https://emojipedia-us.s3.amazonaws.com/thumbs/240/apple/118/printer_1f5a8.png',
    type: 'SINGLE PAGE APPLICATION'
  }
]

class ClientList extends React.Component {
  render() {
    return (
      <div>
        <Heading1>Clients</Heading1>
        <div>Setup a mobile, web or IoT application to use Auth0 for Authentication.</div>
        <br />
        <br />
        <List>
          {clients.map(client => (
            <Stack key={client.id}>
              <Logo />
              <div>
                <Link href={`/clients/${client.id}`}>{client.name}</Link>
                <div>{client.type}</div>
              </div>
              <div>
                Client Id: <Code>{client.id}</Code>
              </div>
              <Stack>
                <Button icon="copy" />
                <Button icon="copy" />
                <Button icon="copy" />
                <Button icon="copy" />
              </Stack>
            </Stack>
          ))}
        </List>
      </div>
    )
  }
}

export default ClientList
