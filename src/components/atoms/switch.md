Switch (by default off)

```
<Switch />
```

Custom accessibility labels:

```
<Switch accessibleLabels={['ON', 'OFF']} />
```

Default state on:

```
<Switch on />
```

Lock switch state

```
<Switch on readOnly />
```
