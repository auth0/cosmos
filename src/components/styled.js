import styled, {
  keyframes,
  css,
  injectGlobal,
  StyledComponent,
  ThemeProvider
} from 'styled-components'

export default styled
module.exports = {
  keyframes,
  css,
  injectGlobal,
  StyledComponent,
  ThemeProvider
}
