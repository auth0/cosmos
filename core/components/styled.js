import styled, {
  keyframes,
  css,
  injectGlobal,
  StyledComponent,
  ThemeProvider
} from 'styled-components'

export default styled
export {
  keyframes,
  css,
  injectGlobal,
  StyledComponent,
  ThemeProvider
}
