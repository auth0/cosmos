import styled from '@auth0/cosmos/styled'

const Left = styled.div`
  float: left;
`
const Right = styled.div`
  float: right;
`
const Clear = styled.div`
  clear: both;
`

export { Left, Right, Clear }
